---
'@astryxdesign/core': patch
---

[chore] Thumbnail: the image hairline now uses a neutral translucent edge (`rgba(0,0,0,.0625)` in light mode, `rgba(255,255,255,.25)` in dark mode) instead of the theme-tinted border token, so it reads consistently across images and themes.

@kentonquatman
